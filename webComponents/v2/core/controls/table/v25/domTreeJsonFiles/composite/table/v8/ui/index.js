import { finalHydratedSpecTree } from "../index.js";
// import buildSpecElement from "../domCreation/index.js";

const tableContainer = document.getElementById("tableContainer");

if (tableContainer) {
    console.log("finalHydratedSpecTree : ", finalHydratedSpecTree);

    // const newTbodyNodes = domCreationFuncs.versions[domCreationFuncs.maxVersion](finalHydratedSpecTree);

    // console.log("jjjjjjjjj : ", newTbodyNodes);

};